import 'package:flutter/material.dart';

class Relatorios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return new Center(
      child: new Text("Relatorios"),
    );
  }

}